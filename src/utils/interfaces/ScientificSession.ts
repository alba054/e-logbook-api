export interface IPostScientificSessionPayload {
  notes?: string;
  attachment?: string;
  supervisorId: string;
  sessionType: string;
  topic: string;
  title: string;
  reference: string;
  role: string;
}
